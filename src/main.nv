// main.nv — rand-nv is a library, so there is no program here.
//
// A manifest names an entry point whether or not anything runs it, and
// a dependency's entry point is never built.  This file is that name,
// and it says what to do instead.

fn main() [io]
    println("rand-nv: a library — depend on it and `use rng`")
